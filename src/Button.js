import React from "react";

export default React.createClass({
    displayName: "Button",
    render: function() {

        return (
            <button style={{background: "red"}}>
                Button
            </button>
        )
    }
})
