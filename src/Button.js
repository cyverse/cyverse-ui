import React from "react";

export default React.createClass({
    render: function() {

        return (
            <button style={{background: "red"}}>
                Button
            </button>
        )
    }
})
